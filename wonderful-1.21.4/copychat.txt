Гифка на фоне почему-то стала зелёная и отдалённо выглядит нормально (что-то не так где-то видимо) поменяй шрифт на более красивый (прямой не пиксельный) грубо говоря читабельный
+ Thought: 6.5s
$ & "C:\Program Files\Java\jdk-21\bin\javap.exe" -classpath "$env:USERPROFILE\.gradle\caches\fabric-loom\minecraftMaven\net\minecraft\minecraft-merged\1.21.4-net.fabricmc.yarn.1_21_4.1.21.4+build.8-v2\minecraft-merged-1.21.4-net.fabricmc.yarn.1_21_4.1.21.4+build.8-v2.jar" net.minecraft.client.texture.NativeImage | Select-String -Pattern "setColor|getColor|Format|NativeImage\(" | Out-String
public net.minecraft.client.texture.NativeImage(int, int, boolean);
  public net.minecraft.client.texture.NativeImage(net.minecraft.client.texture.NativeImage$Format, int, int, boolean);
  public static net.minecraft.client.texture.NativeImage read(net.minecraft.client.texture.NativeImage$Format, java.io.
InputStream) throws java.io.IOException;
  public static net.minecraft.client.texture.NativeImage read(net.minecraft.client.texture.NativeImage$Format, java.nio
.ByteBuffer) throws java.io.IOException;
  public net.minecraft.client.texture.NativeImage$Format getFormat();
  public int getColorArgb(int, int);
  public void setColorArgb(int, int, int);
+ Thought: 392ms
$ & "C:\Program Files\Java\jdk-21\bin\javap.exe" -classpath "$env:USERPROFILE\.gradle\caches\fabric-loom\minecraftMaven\net\minecraft\minecraft-merged\1.21.4-net.fabricmc.yarn.1_21_4.1.21.4+build.8-v2\minecraft-merged-1.21.4-net.fabricmc.yarn.1_21_4.1.21.4+build.8-v2.jar" net.minecraft.client.texture.NativeImage\$Format | Out-String
Error: class not found: net.minecraft.client.texture.NativeImage\
+ Thought: 81ms
$ & "C:\Program Files\Java\jdk-21\bin\javap.exe" -classpath "$env:USERPROFILE\.gradle\caches\fabric-loom\minecraftMaven\net\minecraft\minecraft-merged\1.21.4-net.fabricmc.yarn.1_21_4.1.21.4+build.8-v2\minecraft-merged-1.21.4-net.fabricmc.yarn.1_21_4.1.21.4+build.8-v2.jar" 'net.minecraft.client.texture.NativeImage$Format' | Out-String
Compiled from "NativeImage.java"
public final class net.minecraft.client.texture.NativeImage$Format extends java.lang.Enum<net.minecraft.client.texture.NativeImage$Format> {
  public static final net.minecraft.client.texture.NativeImage$Format RGBA;
  public static final net.minecraft.client.texture.NativeImage$Format RGB;
  public static final net.minecraft.client.texture.NativeImage$Format LUMINANCE_ALPHA;
  public static final net.minecraft.client.texture.NativeImage$Format LUMINANCE;
  final int channelCount;
  public static net.minecraft.client.texture.NativeImage$Format[] values();
  public static net.minecraft.client.texture.NativeImage$Format valueOf(java.lang.String);
  public int getChannelCount();
…
Click to expand
+ Thought: 9.2s
$ & "C:\Program Files\Java\jdk-21\bin\javap.exe" -classpath "$env:USERPROFILE\.gradle\caches\fabric-loom\minecraftMaven\net\minecraft\minecraft-merged\1.21.4-net.fabricmc.yarn.1_21_4.1.21.4+build.8-v2\minecraft-merged-1.21.4-net.fabricmc.yarn.1_21_4.1.21.4+build.8-v2.jar" net.minecraft.client.texture.NativeImageBackedTexture | Out-String
Compiled from "NativeImageBackedTexture.java"
public class net.minecraft.client.texture.NativeImageBackedTexture extends net.minecraft.client.texture.AbstractTexture implements net.minecraft.client.texture.DynamicTexture {
  public net.minecraft.client.texture.NativeImageBackedTexture(net.minecraft.client.texture.NativeImage);
  public net.minecraft.client.texture.NativeImageBackedTexture(int, int, boolean);
  public void upload();
  public net.minecraft.client.texture.NativeImage getImage();
  public void setImage(net.minecraft.client.texture.NativeImage);
  public void close();
  public void save(net.minecraft.util.Identifier, java.nio.file.Path) throws java.io.IOException;
  static {};
…
Click to expand
+ Thought: 7.4s
✱Grep "CFont|FontRenderer|TTFont|ttf|getStringWidth|drawString" in src\main\java\fun\popka (100 matches)
+ Thought: 406ms
→Read src\main\java\fun\popka\api\utils\render\fonts\ttf\Fonts.java 
→Read src\main\java\fun\popka\mixin\FontInitMixin.java 
+ Thought: 1.5s
✱Glob "src/main/resources/assets/popka/fonts/ttf/*.ttf" in . (5 matches)
+ Thought: 3.7s
$ & "C:\Program Files\Java\jdk-21\bin\javap.exe" -classpath "$env:USERPROFILE\.gradle\caches\fabric-loom\minecraftMaven\net\minecraft\minecraft-merged\1.21.4-net.fabricmc.yarn.1_21_4.1.21.4+build.8-v2\minecraft-merged-1.21.4-net.fabricmc.yarn.1_21_4.1.21.4+build.8-v2.jar" net.minecraft.client.texture.AbstractTexture | Out-String
Compiled from "AbstractTexture.java"
public abstract class net.minecraft.client.texture.AbstractTexture implements java.lang.AutoCloseable {
  public static final int DEFAULT_ID;
  protected int glId;
  protected boolean bilinear;
  public net.minecraft.client.texture.AbstractTexture();
  public void setClamp(boolean);
  public void setFilter(net.minecraft.util.TriState, boolean);
  public void setFilter(boolean, boolean);
  public int getGlId();
…
Click to expand
+ Thought: 5.4s
→Read src\main\java\fun\popka\visuals\ui\mainmenu\GifTexture.java 
+ Thought: 7.6s
# Wrote src\main\java\fun\popka\visuals\ui\mainmenu\GifTexture.java
package fun.popka.visuals.ui.mainmenu;

import net.minecraft.client.MinecraftClient;
import net.minecraft.client.texture.NativeImage;
import net.minecraft.client.texture.NativeImageBackedTexture;
import net.minecraft.util.Identifier;

import javax.imageio.ImageIO;
import javax.imageio.ImageReader;
import javax.imageio.metadata.IIOMetadata;
import javax.imageio.metadata.IIOMetadataNode;
import javax.imageio.stream.ImageInputStream;
import java.awt.*;
import java.awt.image.BufferedImage;
import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.InputStream;
import java.util.ArrayList;
import java.util.List;

public class GifTexture {

    private static final int DEFAULT_DELAY = 100;
    private static final int DISPOSAL_NONE = 0;
    private static final int DISPOSAL_DO_NOT_DISPOSE = 1;
    private static final int DISPOSAL_RESTORE_TO_BACKGROUND = 2;
    private static final int DISPOSAL_RESTORE_TO_PREVIOUS = 3;

    private final Identifier resourceId;
    private Identifier textureId;
    private NativeImageBackedTexture texture;
    private final List<Frame> frames = new ArrayList<>();
    private boolean loaded = false;
    private float timer = 0;
    private int currentFrame = 0;
    private boolean loopForever = true;
    private int loopCount = 0;
    private int loopsDone = 0;

    public GifTexture(Identifier resourceId) {
        this.resourceId = resourceId;
        load();
    }

    private void load() {
        try (InputStream stream = MinecraftClient.getInstance().getResourceManager().getResource(resourceId).get().getInputStream()) {
            ImageInputStream imageStream = ImageIO.createImageInputStream(stream);
            ImageReader reader = ImageIO.getImageReadersByFormatName("gif").next();
            reader.setInput(imageStream);

            int width = getInt(reader.getStreamMetadata(), "LogicalScreenDescriptor", "logicalScreenWidth");
            int height = getInt(reader.getStreamMetadata(), "LogicalScreenDescriptor", "logicalScreenHeight");
            if (width <= 0 || height <= 0) {
                width = 1;
                height = 1;
            }

            int frameCount = reader.getNumImages(true);
            BufferedImage canvas = new BufferedImage(width, height, BufferedImage.TYPE_INT_ARGB);
            BufferedImage prevCanvas = null;
            Graphics2D canvasG = canvas.createGraphics();
            canvasG.setComposite(AlphaComposite.Src);

            for (int i = 0; i < frameCount; i++) {
                BufferedImage rawFrame = reader.read(i);
                GifFrameInfo info = parseFrameInfo(reader.getImageMetadata(i));

                if (info.disposalMethod == DISPOSAL_RESTORE_TO_PREVIOUS && prevCanvas != null) {
                    canvasG.drawImage(prevCanvas, 0, 0, null);
                }

                if (info.disposalMethod != DISPOSAL_RESTORE_TO_PREVIOUS) {
                    prevCanvas = new BufferedImage(width, height, BufferedImage.TYPE_INT_ARGB);
                    prevCanvas.createGraphics().drawImage(canvas, 0, 0, null);
                }

                canvasG.drawImage(rawFrame, info.x, info.y, null);

                BufferedImage frameCopy = new BufferedImage(width, height, BufferedImage.TYPE_INT_ARGB);
                frameCopy.createGraphics().drawImage(canvas, 0, 0, null);
                frames.add(new Frame(frameCopy, info.delay));

                if (info.disposalMethod == DISPOSAL_RESTORE_TO_BACKGROUND) {
                    canvasG.setColor(new Color(0, 0, 0, 0));
                    canvasG.fillRect(info.x, info.y, rawFrame.getWidth(), rawFrame.getHeight());
                } else if (info.disposalMethod == DISPOSAL_RESTORE_TO_PREVIOUS) {
                    canvasG.drawImage(prevCanvas, 0, 0, null);
                }
            }

            canvasG.dispose();
            reader.dispose();
            imageStream.close();

            if (!frames.isEmpty()) {
                NativeImage nativeImage = createNativeImage(frames.get(0).image);
                texture = new NativeImageBackedTexture(nativeImage);
                texture.setFilter(true, false);
                textureId = Identifier.of("popka", "dynamic/mainmenu_bg");
                MinecraftClient.getInstance().getTextureManager().registerTexture(textureId, texture);
                loaded = true;
            }
        } catch (Exception e) {
            e.printStackTrace();
            loaded = false;
        }
    }

    private static NativeImage createNativeImage(BufferedImage image) throws Exception {
        ByteArrayOutputStream baos = new ByteArrayOutputStream();
        ImageIO.write(image, "PNG", baos);
        byte[] bytes = baos.toByteArray();
        try (ByteArrayInputStream bais = new ByteArrayInputStream(bytes)) {
            return NativeImage.read(NativeImage.Format.RGBA, bais);
        }
    }

    public void update(float delta) {
        if (!loaded || frames.isEmpty()) return;

        timer += delta * 1000f;
        Frame frame = frames.get(currentFrame);
        while (timer >= frame.delay) {
            timer -= frame.delay;
            currentFrame++;
            if (currentFrame >= frames.size()) {
                if (loopForever) {
                    currentFrame = 0;
                } else {
                    currentFrame = frames.size() - 1;
                }
            }
            frame = frames.get(currentFrame);
            NativeImage image = createNativeImage(frame.image);
            texture.setImage(image);
            texture.upload();
        }
    }

    public Identifier getIdentifier() {
        return textureId;
    }

    public boolean isLoaded() {
        return loaded;
    }

    public void close() {
        if (texture != null) {
            texture.close();
        }
    }

    private static int getInt(IIOMetadata metadata, String nodeName, String attrName) {
        if (metadata == null) return 0;
        IIOMetadataNode root = (IIOMetadataNode) metadata.getAsTree("javax_imageio_gif_stream_1.0");
        if (root == null) return 0;
        IIOMetadataNode node = (IIOMetadataNode) root.getElementsByTagName(nodeName).item(0);
        if (node == null) return 0;
        try {
            return Integer.parseInt(node.getAttribute(attrName));
        } catch (NumberFormatException e) {
            return 0;
        }
    }

    private static GifFrameInfo parseFrameInfo(IIOMetadata metadata) {
        IIOMetadataNode root = (IIOMetadataNode) metadata.getAsTree("javax_imageio_gif_image_1.0");
        IIOMetadataNode gce = (IIOMetadataNode) root.getElementsByTagName("GraphicControlExtension").item(0);
        IIOMetadataNode img = (IIOMetadataNode) root.getElementsByTagName("ImageDescriptor").item(0);

        int delay = DEFAULT_DELAY;
        int disposal = DISPOSAL_NONE;
        int x = 0;
        int y = 0;

        if (gce != null) {
            String delayTime = gce.getAttribute("delayTime");
            try {
                delay = Integer.parseInt(delayTime) * 10;
            } catch (NumberFormatException ignored) {
            }
            String disposalName = gce.getAttribute("disposalMethod");
            if (disposalName != null) {
                try {
                    disposal = Integer.parseInt(disposalName);
                } catch (NumberFormatException ignored) {
                }
            }
        }

        if (img != null) {
            try {
                x = Integer.parseInt(img.getAttribute("imageLeftPosition"));
                y = Integer.parseInt(img.getAttribute("imageTopPosition"));
            } catch (NumberFormatException ignored) {
            }
        }

        if (delay <= 0) delay = DEFAULT_DELAY;
        return new GifFrameInfo(delay, disposal, x, y);
    }

    private record GifFrameInfo(int delay, int disposalMethod, int x, int y) {
    }

    private record Frame(BufferedImage image, int delay) {
    }
}

+ Thought: 167ms
→Read src\main\java\fun\popka\visuals\ui\mainmenu\PopkaTitleScreen.java 
+ Thought: 555ms
← Edit src\main\java\fun\popka\visuals\ui\mainmenu\PopkaTitleScreen.java
package fun.popka.visuals.ui.mainmenu;

import fun.popka.api.utils.render.RenderUtils;


import net.minecraft.client.gui.DrawContext;
import net.minecraft.client.gui.screen.Screen;
import net.minecraft.client.gui.screen.multiplayer.MultiplayerScreen;
import net.minecraft.client.gui.screen.option.OptionsScreen;
import net.minecraft.client.gui.widget.ButtonWidget;
import net.minecraft.text.Text;
import net.minecraft.util.Identifier;


import java.util.ArrayList;
import java.util.List;

public class PopkaTitleScreen extends Screen {
← Edit src\main\java\fun\popka\visuals\ui\mainmenu\PopkaTitleScreen.java

private final List<ButtonSpec> buttonSpecs = new ArrayList<>();
private GifTexture backgroundTexture;



public PopkaTitleScreen() {
    super(Text.literal("PopkaVisuals"));


    backgroundTexture = new GifTexture(BACKGROUND_GIF);
}

@Override
← Edit src\main\java\fun\popka\visuals\ui\mainmenu\PopkaTitleScreen.java
public void render(DrawContext context, int mouseX, int mouseY, float delta) {
    this.renderBackground(context, mouseX, mouseY, delta);
    super.render(context, mouseX, mouseY, delta);

    int titleScale = 3;
    int titleWidth = this.client.textRenderer.getWidth(TITLE_TEXT) * titleScale;
    int titleX = (this.width - titleWidth) / 2;
    int titleY = (this.height / 2) - 90;

    context.getMatrices().push();
    context.getMatrices().translate(titleX, titleY, 0);
    context.getMatrices().scale(titleScale, titleScale, 1);
    context.drawTextWithShadow(this.client.textRenderer, TITLE_TEXT, 0, 0, 0xFFFFFFFF);
    context.getMatrices().pop();
}

@Override
public void removed() {
+ Thought: 339ms
$ .\gradlew compileJava
> Configure project :
Fabric Loom: 1.15.5

C:\Users\леша\Desktop\popkavisualssrc1.21.4\wonderful-1.21.4\src\main\java\fun\popka\visuals\ui\mainmenu\GifTexture.java:134: error: unreported exception Exception; must be caught or declared to be thrown
            NativeImage image = createNativeImage(frame.image);
                                                 ^
1 error
> Task :compileJava FAILED

[Incubating] Problems report is available at: file:///C:/Users/%D0%BB%D0%B5%D1%88%D0%B0/Desktop/popkavisualssrc1.21.4/wonderful-1.21.4/build/reports/problems/problems-report.html
…
Click to expand
+ Thought: 94ms
← Edit src\main\java\fun\popka\visuals\ui\mainmenu\GifTexture.java
                currentFrame = frames.size() - 1;
            }
        }
        frame = frames.get(currentFrame);
        NativeImage image = createNativeImage(frame.image);
        texture.setImage(image);
        texture.upload();




    }
}

public Identifier getIdentifier() {
Payment Required: Insufficient token balance. Please top up at jet-night.com
Продолжи делать
Payment Required: Insufficient token balance. Please top up at jet-night.com